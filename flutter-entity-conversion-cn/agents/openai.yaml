interface:
  display_name: "Flutter 实体转换"
  short_description: "Flutter 响应包装对象与实体转换模板规范"
  default_prompt: "使用 $flutter-entity-conversion-cn 生成响应包装对象并根据后端 JSON 生成 Flutter 实体。"

policy:
  allow_implicit_invocation: true
